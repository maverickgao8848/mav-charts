import { describe, expect, it } from "vitest"; import { getPercentStackedMotion } from "../motion"; import { resolvePercentStackedAnimation } from "../index"; describe("C09 motion", () => { it("stages series", () => expect(getPercentStackedMotion("signal", true, 1)).toMatchObject({ isAnimationActive: true, animationBegin: 90 })); it("supports reduced/capture", () => { expect(resolvePercentStackedAnimation(undefined, true)).toBe(false); expect(resolvePercentStackedAnimation(false, false)).toBe(false); }); });
