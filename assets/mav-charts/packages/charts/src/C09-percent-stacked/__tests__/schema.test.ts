import { describe, expect, it } from "vitest"; import { buildPercentStackedGeometry, getPercentPair, getPercentStackedSegmentHeight, mapPercentStackedY, validatePercentStackedData } from "../schema"; import { percentStackedEdgeCases, percentStackedExample } from "../example-data";
describe("C09 percent geometry", () => { it("normalizes raw totals and retains raw values", () => { const row = buildPercentStackedGeometry([{ label: "Arbitrary", value: 2, comparison: 3 }])[0]; expect(row).toMatchObject({ total: 5, valueShare: .4, comparisonShare: .6, valueStart: 0, valueEnd: 40, comparisonStart: 40, comparisonEnd: 100, valuePercent: 40, comparisonPercent: 60 }); }); it("rounds integer pairs deterministically to 100", () => { expect(getPercentPair(1 / 3)).toEqual([33, 67]); expect(getPercentPair(.995)).toEqual([100, 0]); for (const row of buildPercentStackedGeometry(percentStackedExample)) expect(row.valuePercent! + row.comparisonPercent!).toBe(100); }); it("makes incomplete rows whole-column gaps", () => { expect(buildPercentStackedGeometry(percentStackedEdgeCases.missingValue)[0]).toMatchObject({ complete: false, total: null, valueShare: null, comparisonShare: null }); }); it("supports zero segments but rejects zero total, negative and nonfinite", () => { expect(validatePercentStackedData(percentStackedEdgeCases.zeroSegment).valid).toBe(true); expect(validatePercentStackedData(percentStackedEdgeCases.zeroTotal).valid).toBe(false); expect(validatePercentStackedData(percentStackedEdgeCases.negative).valid).toBe(false); expect(validatePercentStackedData(percentStackedEdgeCases.nonfinite).valid).toBe(false); }); it("maps fixed scale and exact heights", () => { expect(mapPercentStackedY(25, [400, 100])).toBe(325); expect(getPercentStackedSegmentHeight(40, 100, 300)).toBe(180); }); it("rejects duplicate labels", () => expect(validatePercentStackedData(percentStackedEdgeCases.duplicate).valid).toBe(false)); });
