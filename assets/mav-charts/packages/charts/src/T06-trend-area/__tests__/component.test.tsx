import axe from"axe-core";import{fireEvent,render,screen}from"@testing-library/react";import{renderToString}from"react-dom/server";import{describe,expect,it,vi}from"vitest";import{TrendAreaChart,trendAreaEdgeCases,trendAreaExample}from"../index";
describe("T06 component",()=>{it("renders states",()=>{const{rerender}=render(<TrendAreaChart data={trendAreaExample} animate={false}/>);expect(screen.getByRole("article")).toHaveAttribute("data-state","ready");rerender(<TrendAreaChart data={[]} animate={false}/>);expect(screen.getByRole("article")).toHaveAttribute("data-state","empty");rerender(<TrendAreaChart data={trendAreaEdgeCases.invalid} animate={false}/>);expect(screen.getByRole("article")).toHaveAttribute("data-state","invalid")});it("retains missing and zero baseline",()=>{render(<TrendAreaChart data={trendAreaEdgeCases.missing} animate={false}/>);const table=screen.getByRole("table");expect(table).toHaveTextContent("Feb");expect(table).toHaveTextContent("Missing");expect(table).toHaveTextContent("Area baseline")});it("supports keyboard status",()=>{render(<TrendAreaChart data={trendAreaEdgeCases.missing} animate={false} unit=" pts"/>);const chart=screen.getByRole("group",{name:"Trend area interactive chart"});fireEvent.focus(chart);expect(screen.getByRole("status")).toHaveTextContent("area measured from zero");fireEvent.keyDown(chart,{key:"ArrowRight"});expect(screen.getByRole("status")).toHaveTextContent("area and outline break")});it("renders legend and full labels",()=>{render(<TrendAreaChart data={trendAreaEdgeCases.longLabel} animate={false}/>);expect(screen.getByRole("list",{name:"Trend area legend"})).toHaveTextContent("area from zero");expect(screen.getByRole("table")).toHaveTextContent("First enterprise reporting interval")});it("honors reduced motion",()=>{const m=vi.spyOn(window,"matchMedia").mockReturnValue({matches:true,media:"",onchange:null,addListener:vi.fn(),removeListener:vi.fn(),addEventListener:vi.fn(),removeEventListener:vi.fn(),dispatchEvent:vi.fn()});render(<TrendAreaChart/>);expect(screen.getByRole("group",{name:"Trend area interactive chart"})).toHaveAttribute("data-trend-area-animation","false");m.mockRestore()});it("passes axe and SSR",async()=>{const{container}=render(<TrendAreaChart animate={false}/>);expect((await axe.run(container,{rules:{"color-contrast":{enabled:false}}})).violations).toEqual([]);expect(()=>renderToString(<TrendAreaChart animate={false}/>)).not.toThrow()})});
