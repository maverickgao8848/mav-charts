import { describe, expect, it } from "vitest"; import { getRegressionMotion } from "../motion"; describe("D05 motion", () => { it("supports observable and static modes", () => { expect(getRegressionMotion("signal", true)).toMatchObject({ isAnimationActive: true, animationDuration: 800 }); expect(getRegressionMotion("signal", false)).toMatchObject({ isAnimationActive: false, animationDuration: 0 }); }); });
