import{describe,expect,it}from"vitest";import{resolveTrendAreaAnimation}from"../index";import{getTrendAreaMotion}from"../motion";describe("T06 motion",()=>{it("uses system timing",()=>expect(getTrendAreaMotion("signal",true)).toMatchObject({isAnimationActive:true,animationDuration:700}));it("resolves capture/reduced",()=>{expect(resolveTrendAreaAnimation(false,false)).toBe(false);expect(resolveTrendAreaAnimation(undefined,true)).toBe(false);expect(resolveTrendAreaAnimation(undefined,false)).toBe(true)})});
