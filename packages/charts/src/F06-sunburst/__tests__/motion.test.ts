import { describe, expect, it } from "vitest"; import { getSunburstMotion } from "../motion"; describe("F06 motion", () => { it("provides observable entry and static first frame", () => { expect(getSunburstMotion("signal", true)).toMatchObject({ enabled: true, durationMs: 720 }); expect(getSunburstMotion("digital", false)).toMatchObject({ enabled: false, durationMs: 0, animationName: "none" }); }); });
